[project]
name = "orders"
version = "0.0.1"
authors = [
  { name = "Michael Schumacher" }
]
description = "A small demo application for animating points"
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
